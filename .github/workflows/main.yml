name: CI
on: [push, pull_request]

jobs:
  cypress-e2e:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build application
        run: bun run build

      - name: Run Cypress E2E tests
        uses: cypress-io/github-action@v6
        with:
          install: false
          start: bun run start
          wait-on: "http://localhost:3000"

  # Cypress Component Tests (commented out - add when ready)
  # cypress-component:
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Bun
  #       uses: oven-sh/setup-bun@v2
  #       with:
  #         bun-version: latest
  #
  #     - name: Install dependencies
  #       run: bun install
  #
  #     - name: Run Cypress Component tests
  #       uses: cypress-io/github-action@v6
  #       with:
  #         install: false
  #         component: true

  # Vitest Unit Tests (commented out - add when ready)
  # vitest:
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Bun
  #       uses: oven-sh/setup-bun@v2
  #       with:
  #         bun-version: latest
  #
  #     - name: Install dependencies
  #       run: bun install
  #
  #     - name: Run Vitest tests
  #       run: bun run test

  # Storybook Tests (commented out - add when ready)
  # storybook:
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Bun
  #       uses: oven-sh/setup-bun@v2
  #       with:
  #         bun-version: latest
  #
  #     - name: Install dependencies
  #       run: bun install
  #
  #     - name: Build Storybook
  #       run: bun run build-storybook
  #
  #     - name: Run Storybook tests
  #       run: bun run test-storybook
